# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Private, local AI-instruction entry point -- not shared via version control.
# docs/ IS tracked (see docs/git/git-workflow.md); only this file is excluded.
claude.md

# Test artifacts
coverage
playwright-report
test-results
blob-report
.vitest-cache

# Env files -- never commit secrets. Only the committed .example is tracked.
.env
.env.*
!.env.example

# Wrangler local state
.wrangler

# Husky's internal shim, regenerated by `npm run prepare` on install
.husky/_

# Claude Code local session state -- not project source
.claude